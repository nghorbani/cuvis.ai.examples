defaults:
  - _self_
  - eval: efficientad
  - reporting: efficientad

inputs:
  preds_dir: ./work_dirs/exp/preds
  gts_dir: ./work_dirs/exp/gts

outputs:
  out_dir: ./work_dirs/exp/reports

work_dir: ${oc.env:WORK_DIR, ./work_dirs/exp}
