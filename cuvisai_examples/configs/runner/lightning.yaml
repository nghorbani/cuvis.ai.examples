type: "LightningRunner"
params:
  logger: "tensorboard"
  save_dir: "./work_dirs"
  max_epochs: ${trainer.max_epochs}
  accelerator: ${trainer.accelerator}
  enable_progress_bar: ${trainer.enable_progress_bar}
